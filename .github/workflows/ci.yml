name: CI

on:
  push:
    branches: [main, master, finaliseapp]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Tests + grounding audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dev deps
        run: pip install -r requirements-dev.txt
      - name: Run test suite
        run: python -m pytest tests/ -q

  # Optional deploy-on-green — enable by adding these repo secrets and uncommenting:
  #   GCP_WORKLOAD_IDENTITY_PROVIDER, GCP_SERVICE_ACCOUNT  (Workload Identity Federation)
  # deploy:
  #   needs: test
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   permissions: { contents: read, id-token: write }
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: google-github-actions/auth@v2
  #       with:
  #         workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
  #         service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
  #     - uses: google-github-actions/setup-gcloud@v2
  #     - run: gcloud run deploy pathfinder --source . --region asia-south1 --quiet
